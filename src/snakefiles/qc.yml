# Environment for qc subworkflow
---
channels:
    - conda-forge
    - bioconda
    - defaults
dependencies:
    - trimmomatic=0.35  # Above breaks the trick of process substitution
    # - gzip  # gzip should be installed
